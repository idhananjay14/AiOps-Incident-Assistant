[project]
name = "aiops-incident-assistant"
version = "0.1.0"
description = "Evidence-driven AIOps incident response platform"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
